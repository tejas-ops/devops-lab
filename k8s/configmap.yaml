apiVersion: v1
kind: ConfigMap
metadata:
  name: devops-lab-config
  namespace: devops-lab
data:
  # These are non-sensitive environment variables
  # Sensitive values (passwords, tokens) go in a Secret — see secret.yaml
  ENVIRONMENT: "production"
  APP_VERSION: "1.0.0"
  PORT: "5000"
